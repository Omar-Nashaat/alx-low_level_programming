#include "main.h"

/**
 * _isupper - check if c is uppercase
 *
 * @c: input for function
 *
 * Return: 1 if c is uppercase , 0 otherwise
*/

int main(int c)
{
	if (c >= 65 && c <= 90)
	return (1);
	else
	return (0);
}
